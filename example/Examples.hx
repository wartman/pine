// import async.Suspense.suspense;
// import chat.ChatApp.chatApp;
// import counter.Counter.counter;
// import counter.GlobalCounter.globalCounter;
import hydrate.Hydrate.hydrateRoot;
import todo.Todo.todoRoot;

function main() {
  todoRoot();
  hydrateRoot();
  // chatApp();
  // counter();
  // globalCounter();
  // suspense();
}
