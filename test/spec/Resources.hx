package spec;

class Resources extends Suite {
  function execute() {
    describe('pine.Resource', () -> {
      it('loads a resource async');
      it('correctly lets you know when its loading');
    });
  }
}
